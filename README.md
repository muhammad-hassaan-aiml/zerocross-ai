# ZeroCross
Ultimate Tic-Tac-Toe engine with a native C++ ruleset, pybind11 integration, and RL training.

**Kaggle notes:** Do not `pip install torch`; rely on Kaggle's preinstalled PyTorch environment to prevent CUDA driver mismatches.